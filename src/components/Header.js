import React from 'react';

export default function Header() {
  return (
    <header>
      <nav>
        <a href="#">Home</a>
        <a href="#">Calculator</a>
        <a href="#">Quote</a>
      </nav>
    </header>
  );
}
